   name: Pull and Run Docker Image

   on:
     workflow_run:
       workflows: ["build_and_push"]
       types:
         - completed

   jobs:
     pull_and_run:
       runs-on: ubuntu-latest

       steps:
         - name: Checkout code
           uses: actions/checkout@v2

         - name: Log in to Docker registry
           uses: docker/login-action@v1
           with:
             registry: docker.io
             username: vikrammupparthi
             password: dckr_pat_wQAkR2ot49kqygLZdDZ_Kw7Kc-w

         - name: Pull Docker image
           run: docker pull my-image-name

         - name: Run Docker container
           run: |
             docker run --rm my-image-name
             # Add any additional commands to check the score here
             python test.py
   
